#!/bin/bash

# My nice alias cheatsheet
# alias alias_name="command_to_run"

# Long format list
alias ll="ls -la"
alias aaa="env"
alias debian="ssh debian@127.0.0.1 -p 2222"
#Generate Tags SystemVerilog
alias gtsv="ctags -e -R --languages=systemverilog"

#git stuff
alias gs="git status ."
alias gtree="git log --graph --oneline --all"
